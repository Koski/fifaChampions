<%= javascript_tag do %>
  window.usersURL = '<%= j users_url %>';
  window.users = <%= raw @users.to_json %>
  window.teams = <%= raw @teams.to_json %>
<% end %>

<%= form_for(@tournament) do |f| %>
  <% if @tournament.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tournament.errors.count, "error") %> prohibited this tournament from being saved:</h2>

      <ul>
      <% @tournament.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <p>Number of participants:
    <select id="participantAmount" value="1"></select>
  </p>
  
  <!-- <select name="tournament[tournament_stats][team_id]">
    <option value="3">
      happi
    </option>
  </select>
    <select>
    <option value="team_id">
      boca
    </option>
  </select> -->
  
  <div id="participantInputs"></div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
